{"id":"thingies-01t","title":"Add serve command with basic HTTP server infrastructure","description":"Create internal/server/ package with:\n- Server struct wrapping net/http.Server\n- Configuration (port, host, auth token)\n- Graceful shutdown support\n- Health check endpoint (/health)\n- CORS middleware for local development\n- Logging middleware\n\nAdd serve.go command in internal/cmd/ that:\n- Accepts --port flag (default 8080)\n- Accepts --host flag (default 0.0.0.0)\n- Opens ThingsDB and passes to handlers\n- Starts server with graceful shutdown on SIGINT/SIGTERM\n\nThis is the foundation all other endpoints build on.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-01-30T07:21:14.623963-05:00","updated_at":"2026-01-30T07:37:29.437909-05:00","closed_at":"2026-01-30T07:37:29.437909-05:00","close_reason":"Implemented in blitz-thingies-01t"}
{"id":"thingies-05d","title":"Add ID column to table list views","description":"Add UUID column to all table list formatters in `internal/output/table.go`.\n\n## Files to modify\n- `internal/output/table.go`\n\n## Functions to update\n1. `FormatTasks()` (line 51) - Add \"ID\" to Headers, add task.UUID to Row\n2. `FormatProjects()` (line 152) - Add \"ID\" to Headers, add proj.UUID to Row  \n3. `FormatAreas()` (line 222) - Add \"ID\" to Headers, add area.UUID to Row\n4. `FormatSearchResults()` (line 303) - Add \"ID\" to Headers, add task.UUID to Row\n\n## Implementation\n- Add \"ID\" as FIRST column (before Title) so it's easy to copy\n- Use truncated UUID (first 8 chars) to save horizontal space\n- Style with dim color to not distract from content\n- Full UUID available via `show` command or `--json`\n\n## Example output\n```\n╭────────┬─────────────────────┬──────┬───────┬────╮\n│ID      │Title                │Status│Project│Area│\n├────────┼─────────────────────┼──────┼───────┼────┤\n│abc12345│My task title        │○     │Bills  │Home│\n╰────────┴─────────────────────┴──────┴───────┴────╯\n```","status":"closed","priority":2,"issue_type":"task","created_at":"2026-01-28T08:07:26.51886-05:00","updated_at":"2026-01-30T07:21:30.106691-05:00","closed_at":"2026-01-30T07:21:30.106691-05:00","close_reason":"implemented","dependencies":[{"issue_id":"thingies-05d","depends_on_id":"thingies-4se","type":"parent-child","created_at":"2026-01-28T08:07:49.619796-05:00","created_by":"daemon"}]}
{"id":"thingies-0wb","title":"Replace --list flag with explicit --project and --area flags in task creation","description":"## Problem\n\nThe `tasks create` command currently uses a `--list` flag that accepts either a project name OR an area name:\n\n```go\ncreateCmd.Flags().StringVar(\u0026createList, \"list\", \"\", \"Project or area name\")\n```\n\nThis is ambiguous - users don't know which type of container they're targeting. It also inconsistent with `tasks list` which uses separate `--project` and `--area` flags.\n\n## Current Behavior\n\n```bash\nthingies tasks create \"My task\" --list \"Work\"  # Is Work a project or area? Unclear.\n```\n\n## Proposed Change\n\nReplace `--list` with two explicit flags:\n\n```go\ncreateCmd.Flags().StringVar(\u0026createProject, \"project\", \"\", \"Add to project (name or UUID)\")\ncreateCmd.Flags().StringVar(\u0026createArea, \"area\", \"\", \"Add to area (name or UUID)\")\n```\n\n## New Behavior\n\n```bash\nthingies tasks create \"My task\" --project \"Bills\"      # Clear: goes in Bills project\nthingies tasks create \"My task\" --area \"Work\"          # Clear: goes in Work area (loose task)\nthingies tasks create \"My task\" --project \"Bills\" --area \"Work\"  # Project wins (area ignored)\n```\n\n## Implementation Notes\n\n1. In `internal/cmd/tasks/create.go`:\n   - Remove `createList` variable\n   - Add `createProject` and `createArea` variables\n   - Update flag definitions\n   - When building `things.AddParams`, set `List` field to project if specified, else area\n\n2. The `things.AddParams.List` field stays as-is (it maps to URL scheme's `list` param)\n\n3. Precedence rule: if both `--project` and `--area` specified, project wins (matches Python API behavior)\n\n4. Update help text to clarify the precedence\n\n5. Print a warning if both flags provided\n\n6. **Validate `--heading` usage**: If `--heading` is specified without `--project`, print error: \"Error: --heading requires --project (headings only exist within projects)\"\n\n## Edge Cases\n\n- `--heading` without `--project`: Should error, not silently ignored\n- Both `--project` and `--area`: Print warning, use project\n\n## Consistency Achieved\n\n| Command | Project Flag | Area Flag |\n|---------|-------------|-----------|\n| `tasks list` | `--project` | `--area` |\n| `tasks create` | `--project` | `--area` |\n\nThis aligns CLI terminology with Things app UI (\"Projects\" and \"Areas\") rather than URL scheme implementation detail (\"list\").","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:27:06.191078-05:00","updated_at":"2026-01-30T07:32:21.068463-05:00","dependencies":[{"issue_id":"thingies-0wb","depends_on_id":"thingies-2hl","type":"blocks","created_at":"2026-01-30T07:28:01.36077-05:00","created_by":"daemon"}]}
{"id":"thingies-1hm","title":"Add write endpoints for task operations","description":"Expose task write operations as REST endpoints:\n\nPOST /tasks - Create task\n  Request body: {title, notes, when, deadline, tags[], list, heading, checklistItems[]}\n  Uses things.BuildAddURL() + things.OpenURL()\n\nPATCH /tasks/:uuid - Update task\n  Request body: {title, notes, when, deadline, tags[], addTags[], areaId, projectId}\n  Uses things.BuildUpdateURL() (requires auth token)\n  Note: areaId/projectId moves require AppleScript (move_task_to_area, move_task_to_project)\n\nPOST /tasks/:uuid/complete - Mark complete\n  Uses things.CompleteTask() AppleScript\n\nPOST /tasks/:uuid/cancel - Mark canceled  \n  Uses things.CancelTask() AppleScript\n\nDELETE /tasks/:uuid - Delete (move to trash)\n  Uses things.DeleteTask() AppleScript\n\nDELETE /tasks - Delete all open tasks (bulk)\n  Uses things.DeleteAllOpenTasks() AppleScript - need to add this function\n\nPOST /tasks/:uuid/move-to-today - Move to Today\n  Uses URL scheme update with when=today\n\nPOST /tasks/:uuid/move-to-someday - Move to Someday\n  Uses URL scheme update with when=someday\n\nAuth token passed via X-Things-Auth-Token header or --auth-token flag on serve.\n\nNote: Task moves to area/project require AppleScript functions that need to be added.","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:21:32.407307-05:00","updated_at":"2026-01-30T07:26:25.769568-05:00","dependencies":[{"issue_id":"thingies-1hm","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:04.252116-05:00","created_by":"daemon"},{"issue_id":"thingies-1hm","depends_on_id":"thingies-8v8","type":"blocks","created_at":"2026-01-30T07:26:39.417287-05:00","created_by":"daemon"}]}
{"id":"thingies-2hl","title":"Support both names and UUIDs for --project and --area flags","description":"## Problem\n\nCurrently the CLI accepts names for projects/areas but the underlying Things operations often need UUIDs. Users should be able to use either:\n\n```bash\n# By name (convenient for humans)\nthingies tasks create \"My task\" --project \"Bills\"\n\n# By UUID (convenient for scripts, unambiguous)\nthingies tasks create \"My task\" --project \"ABC123-DEF456-...\"\n```\n\n## Current Behavior\n\n- `tasks list --project` accepts names (resolved via SQL query)\n- `tasks create --list` passes directly to URL scheme (which accepts both)\n- No consistent detection or handling\n\n## Proposed Change\n\nCreate a utility function to detect and resolve project/area identifiers:\n\n```go\n// internal/db/resolve.go or internal/cmd/shared/resolve.go\n\n// ResolveProjectID returns UUID for a project given name or UUID\n// Returns the input unchanged if it looks like a UUID\n// Looks up by name if it doesn't look like a UUID\nfunc (db *ThingsDB) ResolveProjectID(nameOrUUID string) (string, error) {\n    if looksLikeUUID(nameOrUUID) {\n        return nameOrUUID, nil\n    }\n    return db.GetProjectUUIDByName(nameOrUUID)\n}\n\n// ResolveAreaID returns UUID for an area given name or UUID\nfunc (db *ThingsDB) ResolveAreaID(nameOrUUID string) (string, error) {\n    if looksLikeUUID(nameOrUUID) {\n        return nameOrUUID, nil\n    }\n    return db.GetAreaUUIDByName(nameOrUUID)\n}\n\n// looksLikeUUID checks if string appears to be a UUID\n// Things UUIDs are uppercase, 36 chars with dashes: XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX\nfunc looksLikeUUID(s string) bool {\n    if len(s) != 36 {\n        return false\n    }\n    // Check for dashes in expected positions\n    return s[8] == '-' \u0026\u0026 s[13] == '-' \u0026\u0026 s[18] == '-' \u0026\u0026 s[23] == '-'\n}\n```\n\n## Implementation Notes\n\n1. Check existing code - `internal/db/queries.go` may already have:\n   - `ResolveProjectUUID()` - for prefix matching\n   - `ResolveAreaUUID()` - for prefix matching\n   \n   These do prefix matching on UUIDs. We need name lookup too.\n\n2. Add name lookup queries if not present:\n   ```sql\n   SELECT uuid FROM TMTask WHERE type = 1 AND title = ? AND trashed = 0\n   SELECT uuid FROM TMArea WHERE title = ?\n   ```\n\n3. Handle ambiguity: if multiple projects/areas have same name, either:\n   - Error with \"multiple matches, use UUID\"\n   - Return first match (simpler but less safe)\n\n4. Use resolution in:\n   - `tasks create --project/--area`\n   - `tasks update --project/--area`\n   - `projects create --area`\n\n5. For URL scheme operations: can pass name directly (Things resolves it)\n   For AppleScript operations: need UUID\n\n## Edge Cases\n\n- Name that looks like a UUID (unlikely but possible)\n- Project and area with same name (resolve to correct type based on flag)\n- Non-existent name → clear error message\n- Multiple projects with same name → error or warning","notes":"## Problem\n\nCurrently the CLI accepts names for projects/areas but the underlying Things operations often need UUIDs. Users should be able to use either:\n\n```bash\n# By name (convenient for humans)\nthingies tasks create \"My task\" --project \"Bills\"\n\n# By UUID (convenient for scripts, unambiguous)\nthingies tasks create \"My task\" --project \"ABC123-DEF456-...\"\n```\n\n## Current Behavior\n\n- `tasks list --project` accepts names (resolved via SQL query)\n- `tasks create --list` passes directly to URL scheme (which accepts both)\n- `projects create --area` passes directly to URL scheme (which accepts names)\n- No consistent detection or handling\n\n## Proposed Change\n\nCreate a utility function to detect and resolve project/area identifiers:\n\n```go\n// internal/db/resolve.go or internal/cmd/shared/resolve.go\n\n// ResolveProjectID returns UUID for a project given name or UUID\n// Returns the input unchanged if it looks like a UUID\n// Looks up by name if it doesn't look like a UUID\nfunc (db *ThingsDB) ResolveProjectID(nameOrUUID string) (string, error) {\n    if looksLikeUUID(nameOrUUID) {\n        return nameOrUUID, nil\n    }\n    return db.GetProjectUUIDByName(nameOrUUID)\n}\n\n// ResolveAreaID returns UUID for an area given name or UUID\nfunc (db *ThingsDB) ResolveAreaID(nameOrUUID string) (string, error) {\n    if looksLikeUUID(nameOrUUID) {\n        return nameOrUUID, nil\n    }\n    return db.GetAreaUUIDByName(nameOrUUID)\n}\n\n// looksLikeUUID checks if string appears to be a UUID\n// Things UUIDs are uppercase, 36 chars with dashes: XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX\nfunc looksLikeUUID(s string) bool {\n    if len(s) != 36 {\n        return false\n    }\n    // Check for dashes in expected positions\n    return s[8] == '-' \u0026\u0026 s[13] == '-' \u0026\u0026 s[18] == '-' \u0026\u0026 s[23] == '-'\n}\n```\n\n## Implementation Notes\n\n1. Check existing code - `internal/db/queries.go` may already have:\n   - `ResolveProjectUUID()` - for prefix matching\n   - `ResolveAreaUUID()` - for prefix matching\n   \n   These do prefix matching on UUIDs. We need name lookup too.\n\n2. Add name lookup queries if not present:\n   ```sql\n   SELECT uuid FROM TMTask WHERE type = 1 AND title = ? AND trashed = 0\n   SELECT uuid FROM TMArea WHERE title = ?\n   ```\n\n3. Handle ambiguity: if multiple projects/areas have same name, either:\n   - Error with \"multiple matches, use UUID\"\n   - Return first match (simpler but less safe)\n\n4. Use resolution in:\n   - `tasks create --project/--area` \n   - `tasks update --project/--area`\n   - `projects create --area` (for consistency, though URL scheme handles names)\n\n5. For URL scheme operations: can pass name directly (Things resolves it)\n   For AppleScript operations: need UUID\n\n## Note on URL Scheme vs AppleScript\n\nThe Things URL scheme accepts project/area names directly. However:\n- For `tasks create`: URL scheme, so names work directly\n- For `tasks update --project/--area`: Uses AppleScript which requires UUIDs\n- For `projects create --area`: URL scheme, so names work directly\n\nThis means resolution is mainly needed for the AppleScript-based move operations in `tasks update`.\n\n## Edge Cases\n\n- Name that looks like a UUID (unlikely but possible)\n- Project and area with same name (resolve to correct type based on flag)\n- Non-existent name → clear error message\n- Multiple projects with same name → error or warning","status":"closed","priority":2,"issue_type":"task","created_at":"2026-01-30T07:27:41.431988-05:00","updated_at":"2026-01-30T07:37:22.266663-05:00","closed_at":"2026-01-30T07:37:22.266663-05:00","close_reason":"Implemented in blitz-thingies-2hl"}
{"id":"thingies-376","title":"Add read endpoints for areas and tags","description":"Expose area and tag queries as REST endpoints:\n\nGET /areas - List all visible areas with counts\nGET /areas/:uuid - Single area details\nGET /areas/:uuid/tasks - Loose tasks in area (not in projects)\nGET /areas/:uuid/projects - Projects in area\n\nGET /tags - List all tags with usage counts\nGET /tags/:name/tasks - Tasks with a specific tag\n\nReuse db.ListAreas(), db.GetArea(), db.GetAreaTasks(), db.GetAreaProjects(), db.ListTags(), db.GetTag().","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:21:30.935446-05:00","updated_at":"2026-01-30T07:21:30.935446-05:00","dependencies":[{"issue_id":"thingies-376","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:04.070541-05:00","created_by":"daemon"},{"issue_id":"thingies-376","depends_on_id":"thingies-wdy","type":"blocks","created_at":"2026-01-30T07:26:10.575161-05:00","created_by":"daemon"}]}
{"id":"thingies-3y4","title":"Add upcoming command to show scheduled tasks","description":"## Problem\nThere's no `thingies upcoming` command to show tasks scheduled for future dates.\n\n## Current available commands\n```\nareas, inbox, projects, search, snapshot, tags, tasks, today\n```\n\n## Proposed behavior\n`thingies upcoming` should show tasks that are:\n- Scheduled for future dates (`start = 2` in database)\n- Not completed (`status = 0`)\n- Sorted by scheduled date\n\n## Database query logic\n```sql\nSELECT ... FROM TMTask t\nWHERE t.start = 2        -- scheduled for future\n  AND t.status = 0       -- incomplete\n  AND t.trashed = 0\nORDER BY t.startDate ASC -- binary-packed date sorts correctly\n```\n\n## Implementation\n1. Add `internal/cmd/upcoming.go` (similar to `today.go` and `inbox.go`)\n2. Add query method to `internal/db/queries.go`\n3. Register command in `internal/cmd/root.go`\n\n## Output\nShould show scheduled date (decoded from binary format) and task title, grouped or sorted by date.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-01-27T19:41:24.67211-05:00","updated_at":"2026-01-30T07:21:30.099968-05:00","closed_at":"2026-01-30T07:21:30.099968-05:00","close_reason":"implemented","dependencies":[{"issue_id":"thingies-3y4","depends_on_id":"thingies-z7h","type":"blocks","created_at":"2026-01-27T19:41:33.406113-05:00","created_by":"rob"}]}
{"id":"thingies-4se","title":"Always show IDs in all output","description":"Every task, project, and area must display its UUID in all output formats. IDs are essential for subsequent update/delete/complete operations.\n\n## Problem\nCurrently, list views and snapshot don't show IDs, making it impossible to copy an ID for follow-up commands without using `--json` or `show`.\n\n## Scope\n- Table list views: tasks, projects, areas, search results\n- Snapshot command: today, inbox, areas, projects, tasks\n- Consider ID format: full UUID vs truncated (first 8 chars)","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-01-28T08:07:14.732976-05:00","updated_at":"2026-01-30T07:21:30.109561-05:00","closed_at":"2026-01-30T07:21:30.109561-05:00","close_reason":"implemented"}
{"id":"thingies-6gl","title":"Add read endpoints for views (today/inbox/upcoming/someday/logbook)","description":"Expose the special views as REST endpoints:\n\nGET /today - Today's tasks (db.ListTasks with today filter)\nGET /inbox - Inbox tasks (db.GetInboxTasks)\nGET /anytime - Anytime tasks (db.GetAnytimeTasks - need to add this query)\nGET /upcoming - Upcoming scheduled tasks (db.GetUpcomingTasks)\n  - days (int): optional limit on days ahead\nGET /someday - Someday tasks (db.GetSomedayTasks)\nGET /logbook - Completed tasks (db.GetLogbook)\n  - limit (int): max results, default 50\nGET /deadlines - Tasks with upcoming deadlines (db.GetDeadlines - need to add this query)\n  - days (int): days ahead to check, default 7\n\nThese map directly to existing db functions, but need to add:\n- db.GetAnytimeTasks() \n- db.GetDeadlines(daysAhead int)","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:21:16.947712-05:00","updated_at":"2026-01-30T07:25:57.831112-05:00","dependencies":[{"issue_id":"thingies-6gl","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:03.746686-05:00","created_by":"daemon"},{"issue_id":"thingies-6gl","depends_on_id":"thingies-wdy","type":"blocks","created_at":"2026-01-30T07:26:10.425627-05:00","created_by":"daemon"}]}
{"id":"thingies-6zm","title":"Add --project and --area flags to tasks update for moving tasks","description":"## Problem\n\nThe `tasks update` command cannot move tasks between projects or areas. The Python API supports this via `move_task_to_project()` and `move_task_to_area()` AppleScript functions, but the Go CLI doesn't expose this functionality.\n\n## Current `tasks update` Flags\n\n```go\nupdateCmd.Flags().StringVar(\u0026updateTitle, \"title\", \"\", \"New title\")\nupdateCmd.Flags().StringVar(\u0026updateNotes, \"notes\", \"\", \"New notes\")\nupdateCmd.Flags().StringVar(\u0026updateWhen, \"when\", \"\", \"When to schedule\")\nupdateCmd.Flags().StringVar(\u0026updateDeadline, \"deadline\", \"\", \"Due date\")\nupdateCmd.Flags().StringVar(\u0026updateTags, \"tags\", \"\", \"Tags\")\n// Missing: --project and --area for moving\\!\n```\n\n## Proposed Change\n\nAdd two new flags:\n\n```go\nupdateCmd.Flags().StringVar(\u0026updateProject, \"project\", \"\", \"Move to project (name or UUID)\")\nupdateCmd.Flags().StringVar(\u0026updateArea, \"area\", \"\", \"Move to area (name or UUID)\")\n```\n\n## New Behavior\n\n```bash\n# Move task to a different project\nthingies tasks update abc123 --project \"New Project\"\n\n# Move task to area (removes from project, becomes loose task)\nthingies tasks update abc123 --area \"Personal\"\n\n# Move to project takes precedence (also inherits project's area)\nthingies tasks update abc123 --project \"Bills\" --area \"Work\"  # Goes to Bills project\n```\n\n## Implementation Notes\n\n1. In `internal/cmd/tasks/update.go`:\n   - Add `updateProject` and `updateArea` variables\n   - Add flag definitions\n\n2. In `internal/things/applescript.go`, verify these functions exist (from Python impl):\n   - `MoveTaskToProject(taskUUID, projectUUID string) error`\n   - `MoveTaskToArea(taskUUID, areaUUID string) error`\n   \n   If not, add them using AppleScript:\n   ```applescript\n   tell application \"Things3\"\n       set aToDo to to do id \"TASK_UUID\"\n       set project of aToDo to project id \"PROJECT_UUID\"\n       return \"ok\"\n   end tell\n   ```\n\n3. Need to resolve name → UUID if user provides name:\n   - Check if input looks like UUID (contains dashes, 36 chars)\n   - If not, look up project/area by name using existing db queries\n\n4. Precedence: project wins over area (moving to project also sets task's area to project's area)\n\n5. Can combine with other updates:\n   ```bash\n   thingies tasks update abc123 --title \"New title\" --project \"Bills\"\n   ```\n\n## Consistency Achieved\n\n| Command | Project Flag | Area Flag | Purpose |\n|---------|-------------|-----------|---------|\n| `tasks list` | `--project` | `--area` | Filter |\n| `tasks create` | `--project` | `--area` | Assign |\n| `tasks update` | `--project` | `--area` | Move |","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:27:22.436299-05:00","updated_at":"2026-01-30T07:27:22.436299-05:00","dependencies":[{"issue_id":"thingies-6zm","depends_on_id":"thingies-2hl","type":"blocks","created_at":"2026-01-30T07:28:01.501979-05:00","created_by":"daemon"},{"issue_id":"thingies-6zm","depends_on_id":"thingies-8v8","type":"blocks","created_at":"2026-01-30T07:28:08.043268-05:00","created_by":"daemon"}]}
{"id":"thingies-8cx","title":"Add checklist item support to task queries","description":"Add support for reading checklist items from the database.\n\n1. Add ChecklistItem model to internal/models/:\n   type ChecklistItem struct {\n       UUID      string\n       Title     string\n       Completed bool\n       Index     int\n   }\n\n2. Add GetTaskChecklistItems(taskUUID string) query to internal/db/queries.go:\n   - Query TMChecklistItem table where task = taskUUID\n   - Order by \"index\"\n   - Return []ChecklistItem\n\n3. Update Task model to include ChecklistItems field\n\n4. Optionally fetch checklist items in GetTask() for single task lookups\n\nReference Python: things_client.py _get_checklist_items() method.\n\nNote: Checklist items are already supported for CREATING tasks via URL scheme.\nThis issue is about READING them back from the database.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-01-30T07:27:21.95813-05:00","updated_at":"2026-01-30T07:37:35.196943-05:00","closed_at":"2026-01-30T07:37:35.196943-05:00","close_reason":"Implemented in blitz-thingies-8cx"}
{"id":"thingies-8s7","title":"Add AppleScript functions for heading operations","description":"Add AppleScript functions to internal/things/applescript.go:\n\nfunc DeleteHeading(uuid string) error\n  - Deletes a heading via AppleScript\n  - Tasks under the heading move to project root\n  \nfunc RenameHeading(uuid, newTitle string) error\n  - Renames a heading via AppleScript\n\nBased on Python implementation:\n```applescript\ntell application \"Things3\"\n    set h to to do id \"UUID\"\n    delete h\n    return \"ok\"\nend tell\n```\n\nNote: Headings are stored as type=2 in TMTask, but Things AppleScript treats them as 'to do' objects.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-01-30T07:22:36.14976-05:00","updated_at":"2026-01-30T07:36:55.787936-05:00","closed_at":"2026-01-30T07:36:55.787936-05:00","close_reason":"Implemented in blitz-thingies-8s7"}
{"id":"thingies-8v8","title":"Add AppleScript functions for task move operations","description":"Add AppleScript functions to internal/things/applescript.go:\n\n1. MoveTaskToArea(taskUUID, areaUUID string) error\n   - Moves task to an area using AppleScript\n   - Script: set area of to do id \"X\" to area id \"Y\"\n\n2. MoveTaskToProject(taskUUID, projectUUID string) error\n   - Moves task to a project using AppleScript\n   - Script: set project of to do id \"X\" to project id \"Y\"\n\n3. DeleteAllOpenTasks() (int, error)\n   - Deletes all open tasks (moves to trash)\n   - Returns count of deleted tasks\n   - Script: delete every to do whose status is open\n\nThese are needed by the task write endpoints for moving tasks between areas/projects\nand for bulk delete operations.\n\nReference Python implementation in signalbot2/core/things_client.py:\n- move_task_to_area()\n- move_task_to_project()  \n- delete_all_open_tasks()","status":"closed","priority":2,"issue_type":"task","created_at":"2026-01-30T07:26:34.308356-05:00","updated_at":"2026-01-30T07:37:22.55617-05:00","closed_at":"2026-01-30T07:37:22.55617-05:00","close_reason":"Implemented in blitz-thingies-8v8"}
{"id":"thingies-914","title":"Add CancelProject AppleScript function","description":"Add CancelProject function to internal/things/applescript.go:\n\nfunc CancelProject(uuid string) error\n  - Marks a project as canceled using AppleScript\n  - Script: set status of project id \"UUID\" to canceled\n\nThis mirrors the existing CancelTask() function but for projects.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-01-30T07:26:51.996204-05:00","updated_at":"2026-01-30T07:36:53.243416-05:00","closed_at":"2026-01-30T07:36:53.243416-05:00","close_reason":"Implemented in blitz-thingies-914"}
{"id":"thingies-9ad","title":"Add db.GetProjectHeadings() query function","description":"Add a new query function to internal/db/queries.go:\n\nfunc (db *ThingsDB) GetProjectHeadings(projectUUID string) ([]Heading, error)\n\nQuery TMTask where type=2 (heading) and project=projectUUID.\nReturn list of {UUID, Title, Index} ordered by index.\n\nAlso add Heading model to internal/models/ if not present.\n\nThis is needed by the /projects/:uuid/headings endpoint.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-01-30T07:22:17.907359-05:00","updated_at":"2026-01-30T07:37:22.10912-05:00","closed_at":"2026-01-30T07:37:22.10912-05:00","close_reason":"Implemented in blitz-thingies-9ad"}
{"id":"thingies-a63","title":"Rewrite thingies CLI in Go","description":"Rewrite the Python thingies CLI as a Go CLI with full CRUD access to Things 3. Reads via SQLite, creates/updates via URL scheme, deletes via AppleScript.","notes":"COMPLETED: Go CLI implementation\n- Phase 1 (Core + Read): All list/show commands working\n- Phase 2 (Write): Create, complete, delete working via URL scheme and AppleScript\n- Phase 3 (Polish): Makefile, CLAUDE.md updated\n\nVERIFIED:\n- thingies today/inbox/tasks list/projects list/areas list/tags list\n- thingies search, snapshot\n- thingies tasks create/complete/delete\n- JSON output with --json flag\n- Date handling with Things-specific epoch for startDate/deadline\n\nREMAINING:\n- tasks update requires auth token from TMSettings (plist parsing not implemented)\n- Consider removing Python version after more testing","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-01-25T14:04:10.294652-05:00","updated_at":"2026-01-25T14:23:13.827615-05:00","closed_at":"2026-01-25T14:23:13.827615-05:00","close_reason":"Go CLI implementation complete - all core features working (list, show, create, complete, delete). Ready for testing."}
{"id":"thingies-c1d","title":"Add read endpoints for tasks","description":"Expose existing db query functions as REST endpoints:\n\nGET /tasks - List tasks with query params:\n  - status (incomplete/completed/canceled/all)\n  - area (UUID or name)\n  - project (UUID or name)  \n  - tag (name)\n  - today (bool)\n  - include-future (bool)\n\nGET /tasks/:uuid - Single task details\n\nGET /tasks/search?q=... - Search tasks\n  - q (required): search query\n  - include-completed (bool)\n  - in-notes (bool)\n\nReuse db.ListTasks(), db.GetTask(), db.Search() directly.\nReturn JSON using existing models.TaskJSON structs.","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:21:15.535821-05:00","updated_at":"2026-01-30T07:21:15.535821-05:00","dependencies":[{"issue_id":"thingies-c1d","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:03.599212-05:00","created_by":"daemon"},{"issue_id":"thingies-c1d","depends_on_id":"thingies-8cx","type":"blocks","created_at":"2026-01-30T07:27:25.862828-05:00","created_by":"daemon"}]}
{"id":"thingies-cqm","title":"Fix task update --when to use URL scheme instead of AppleScript","description":"## Problem\nThe `tasks update --when` command uses AppleScript to set `activation date`, but this property is **read-only** in Things 3.\n\n```\nexecution error: Things3 got an error: Can't set activation date of selected to do to date \"Wednesday, January 28, 2026 at 7:37:46 PM\". (-10006)\n```\n\n## Current (broken) code in `applescript.go`\n```go\ncase \"tomorrow\":\n    statements = append(statements, `set activation date of theTodo to (current date) + 1 * days`)\ndefault:\n    statements = append(statements, fmt.Sprintf(`set activation date of theTodo to date \"%s\"`, params.When))\n```\n\n## Solution\nTwo options depending on the date value:\n\n### For predefined lists (today, tomorrow, anytime, someday)\nUse AppleScript `move` command:\n```applescript\nmove theTodo to list \"Today\"\nmove theTodo to list \"Tomorrow\"\nmove theTodo to list \"Anytime\"\nmove theTodo to list \"Someday\"\n```\n\n### For specific dates (YYYY-MM-DD)\nMust use Things URL scheme with auth token:\n```\nthings:///update?id=UUID\u0026auth-token=TOKEN\u0026when=2026-02-15\n```\n\n## Files to change\n- `internal/things/applescript.go`: Use `move to list` for predefined values\n- For specific dates, either:\n  - Fall back to URL scheme (requires implementing auth token retrieval)\n  - Or document as unsupported via AppleScript","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-01-27T19:41:13.333452-05:00","updated_at":"2026-01-30T07:21:30.112265-05:00","closed_at":"2026-01-30T07:21:30.112265-05:00","close_reason":"implemented"}
{"id":"thingies-d1q","title":"Add read endpoints for projects","description":"Expose project queries as REST endpoints:\n\nGET /projects - List all projects\n  - include-completed (bool): include completed/canceled projects\n\nGET /projects/:uuid - Single project details with task counts\n\nGET /projects/:uuid/tasks - Tasks in a project\n  - include-completed (bool)\n\nGET /projects/:uuid/headings - Headings/sections in a project\n\nReuse db.ListProjects(), db.GetProject(), db.GetProjectTasks().\nNeed to add db.GetProjectHeadings() if not present.","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:21:29.92162-05:00","updated_at":"2026-01-30T07:21:29.92162-05:00","dependencies":[{"issue_id":"thingies-d1q","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:03.891262-05:00","created_by":"daemon"},{"issue_id":"thingies-d1q","depends_on_id":"thingies-9ad","type":"blocks","created_at":"2026-01-30T07:22:22.322249-05:00","created_by":"daemon"}]}
{"id":"thingies-d5u","title":"Add write endpoints for project operations","description":"Expose project write operations as REST endpoints:\n\nPOST /projects - Create project\n  Request body: {title, notes, when, deadline, tags[], areaId, todos[], headings[]}\n  Uses things.BuildAddProjectURL() + things.OpenURL()\n  Note: Projects with headings require JSON URL scheme (things:///json)\n\nPATCH /projects/:uuid - Update project\n  Request body: {title, notes, deadline, tags[]}\n  Uses things.UpdateProject() AppleScript\n\nPOST /projects/:uuid/complete - Mark complete\n  Uses things.CompleteProject() AppleScript\n\nPOST /projects/:uuid/cancel - Mark canceled\n  Uses things.CancelProject() AppleScript - need to add this function\n\nDELETE /projects/:uuid - Delete (move to trash)\n  Uses things.DeleteProject() AppleScript","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:21:44.673894-05:00","updated_at":"2026-01-30T07:26:46.216668-05:00","dependencies":[{"issue_id":"thingies-d5u","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:04.397207-05:00","created_by":"daemon"},{"issue_id":"thingies-d5u","depends_on_id":"thingies-914","type":"blocks","created_at":"2026-01-30T07:26:56.474513-05:00","created_by":"daemon"}]}
{"id":"thingies-e14","title":"Add HTTP server (thingies serve)","description":"Add a 'thingies serve' command that exposes the CLI functionality as a REST API.\n\nThis replaces the ad-hoc Python FastAPI server in signalbot2 with a proper Go implementation that:\n- Uses the same database queries as the CLI\n- Returns consistent JSON responses\n- Supports all read operations (tasks, projects, areas, tags, views)\n- Supports all write operations (create, update, complete, delete)\n- Can be run standalone or via pm2/systemd\n- Exposed over Tailscale at https://vega.tailnet.ts.net/\n\nMigrating from Python to Go consolidates everything into one binary and ensures CLI and API stay perfectly in sync.","status":"open","priority":1,"issue_type":"epic","created_at":"2026-01-30T07:21:57.350213-05:00","updated_at":"2026-01-30T07:21:57.350213-05:00"}
{"id":"thingies-gd1","title":"Add --add-tags flag to tasks update command","description":"## Problem\n\nThe Python API and Things URL scheme both support `add-tags` for appending tags without replacing existing ones, but the Go CLI's `tasks update` command doesn't expose this.\n\n## Current State\n\n- `internal/things/urlscheme.go` has `UpdateParams.AddTags` (line 131)\n- `internal/things/urlscheme.go` builds `add-tags` param (line 169)\n- Python API uses `add_tags` in `UpdateTaskRequest` (things_api.py line 143)\n- Go CLI only has `--tags` which replaces all tags\n\n## Proposed Change\n\nAdd `--add-tags` flag to `tasks update`:\n\n```go\n// internal/cmd/tasks/update.go\nupdateCmd.Flags().StringVar(\u0026updateAddTags, \"add-tags\", \"\", \"Tags to add (comma-separated, appends to existing)\")\n```\n\n## Use Case\n\n```bash\n# Current: replaces all tags\nthingies tasks update abc123 --tags \"work,urgent\"\n\n# New: adds tags without removing existing\nthingies tasks update abc123 --add-tags \"urgent\"\n```\n\n## Implementation\n\n1. Add flag variable and registration in `internal/cmd/tasks/update.go`\n2. Pass to AppleScript update function (may need to extend `TaskUpdateParams`)\n\nNote: The URL scheme supports this but the current implementation uses AppleScript for updates. Need to verify AppleScript can append tags vs replace.\n\n## Priority\n\nP3 - Nice to have, not blocking the main flag consistency work.","status":"open","priority":3,"issue_type":"task","created_at":"2026-01-30T07:31:14.011561-05:00","updated_at":"2026-01-30T07:31:14.011561-05:00"}
{"id":"thingies-gxv","title":"Show project ID and heading ID in task details view","description":"In the task details view (thingies tasks show \u003cuuid\u003e), add:\n- Project UUID alongside the project title\n- Heading UUID and title if the task is under a heading\n\nCurrently the output shows project_title but not project_uuid, and heading info may be missing entirely. This makes it harder to use the CLI output for scripting or follow-up operations that need these IDs.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-01-30T07:18:26.666997-05:00","updated_at":"2026-01-30T07:24:14.040864-05:00","closed_at":"2026-01-30T07:24:14.040864-05:00","close_reason":"implemented"}
{"id":"thingies-kcj","title":"Add write endpoints for heading operations","description":"Expose heading management as REST endpoints:\n\nDELETE /headings/:uuid - Delete heading (tasks move to project root)\n  Uses AppleScript delete\n\nPATCH /headings/:uuid - Rename heading\n  Request body: {title}\n  Uses AppleScript to set name\n\nNote: Creating headings on existing projects is not supported by Things AppleScript.\nHeadings can only be created during project creation via the JSON URL scheme.","status":"open","priority":3,"issue_type":"task","created_at":"2026-01-30T07:21:46.171762-05:00","updated_at":"2026-01-30T07:21:46.171762-05:00","dependencies":[{"issue_id":"thingies-kcj","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:04.554883-05:00","created_by":"daemon"},{"issue_id":"thingies-kcj","depends_on_id":"thingies-8s7","type":"blocks","created_at":"2026-01-30T07:22:40.444586-05:00","created_by":"daemon"}]}
{"id":"thingies-nce","title":"Update CLAUDE.md documentation after --list flag removal","description":"## Problem\n\nCLAUDE.md currently shows `--list` flag in examples:\n\n```\nthingies tasks create \"Title\" --list \"Project Name\" --heading \"Section\"\n```\n\nAfter the flag consistency work lands, this needs to be updated.\n\n## Changes Required\n\nUpdate CLAUDE.md examples to use `--project` and `--area`:\n\n```\nthingies tasks create \"Title\" --project \"Project Name\" --heading \"Section\"\nthingies tasks create \"Title\" --area \"Work\"\n```\n\nAlso add documentation for new `tasks update` move functionality:\n\n```\nthingies tasks update \u003cuuid\u003e --project \"New Project\"   # Move to project\nthingies tasks update \u003cuuid\u003e --area \"Work\"             # Move to area (loose task)\n```\n\n## Timing\n\nDo this after thingies-0wb and thingies-6zm are complete.\n\n## Files to Update\n\n- `/Users/rob/Sync/repos/thingies/CLAUDE.md`","status":"open","priority":3,"issue_type":"chore","created_at":"2026-01-30T07:31:23.896358-05:00","updated_at":"2026-01-30T07:31:23.896358-05:00","dependencies":[{"issue_id":"thingies-nce","depends_on_id":"thingies-0wb","type":"blocks","created_at":"2026-01-30T07:31:23.901141-05:00","created_by":"daemon"},{"issue_id":"thingies-nce","depends_on_id":"thingies-6zm","type":"blocks","created_at":"2026-01-30T07:31:23.903315-05:00","created_by":"daemon"}]}
{"id":"thingies-oxf","title":"Add snapshot endpoint","description":"Expose hierarchical snapshot as REST endpoint:\n\nGET /snapshot - Full hierarchical dump for system prompts\n  Returns: {snapshot: string} with formatted text\n\nShould match the Python API's snapshot output:\n- Organizes by: Area → Project → Heading → Task\n- Sections: TODAY, ANYTIME, UPCOMING, SOMEDAY, INBOX\n- Each task shows ID, deadline if set, scheduled date if future\n- Deduplicates tasks across sections\n\nCan reuse logic from internal/cmd/snapshot.go but output as JSON wrapper.","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-30T07:21:47.802234-05:00","updated_at":"2026-01-30T07:21:47.802234-05:00","dependencies":[{"issue_id":"thingies-oxf","depends_on_id":"thingies-01t","type":"blocks","created_at":"2026-01-30T07:22:04.703562-05:00","created_by":"daemon"}]}
{"id":"thingies-pk1","title":"Add IDs to snapshot output","description":"Add IDs to the snapshot command's pretty-print output in `internal/cmd/snapshot.go`.\n\n## File to modify\n- `internal/cmd/snapshot.go`\n\n## Locations in `printSnapshot()` (lines 122-199)\n1. Today tasks (line 143): Add task ID\n2. Inbox tasks (line 152): Add task ID\n3. Area headers (line 164): Add area ID\n4. Project headers (line 169): Add project ID\n5. Project tasks (line 173): Add task ID\n6. Area direct tasks (line 180): Add task ID\n\n## Implementation\n- Show truncated ID (first 8 chars) in dim style after each item\n- Format: `○ Task title                              (abc12345)`\n- For areas/projects with counts, put ID before count: `■ Work (abc12345) (17)`\n- Or: `■ Work (17)                              [abc12345]`\n\n## Example output\n```\n📅 Today\n  ○ Pay rent                                (abc12345)\n  ○ Call mom                                (def67890)\n\n■ Work (abc12345) (17)\n  ▸ Project Name (xyz98765) (3)\n    ○ Task one                              (111aaaaa)\n    ○ Task two                              (222bbbbb)\n```\n\n## Note\nJSON output (`--json`) already includes full UUIDs via TaskJSON/ProjectJSON structs.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-01-28T08:07:38.234137-05:00","updated_at":"2026-01-30T07:21:30.103884-05:00","closed_at":"2026-01-30T07:21:30.103884-05:00","close_reason":"implemented","dependencies":[{"issue_id":"thingies-pk1","depends_on_id":"thingies-4se","type":"parent-child","created_at":"2026-01-28T08:07:49.7927-05:00","created_by":"daemon"}]}
{"id":"thingies-wdy","title":"Add missing db query functions for HTTP server","description":"Add these missing query functions to internal/db/queries.go:\n\n1. GetAnytimeTasks() - Tasks with start=1 (Anytime bucket)\n2. GetDeadlines(daysAhead int) - Tasks with deadlines within N days\n3. GetTasksByTag(tagName string) - Tasks with a specific tag\n\nThese are needed by the HTTP server endpoints but don't currently exist in the Go codebase.\n\nReference the Python implementation in signalbot2/core/things_client.py for the query logic.","status":"in_progress","priority":2,"issue_type":"task","created_at":"2026-01-30T07:26:05.566607-05:00","updated_at":"2026-01-30T07:36:35.128804-05:00"}
{"id":"thingies-wg3","title":"CLI flag consistency: --project/--area standardization","description":"## Overview\n\nStandardize CLI flag naming for project and area references across all commands. Currently there's inconsistency between `--list` (ambiguous) and `--project`/`--area` (explicit).\n\n## Goals\n\n1. **Consistent terminology**: Use `--project` and `--area` everywhere, matching Things app UI\n2. **Clear semantics**: Users always know whether they're targeting a project or area\n3. **Flexible input**: Accept both names and UUIDs for convenience\n4. **Defined precedence**: When both specified, project wins (documented behavior)\n\n## Changes Required\n\n| Command | Current | Proposed |\n|---------|---------|----------|\n| `tasks create` | `--list` (ambiguous) | `--project`, `--area` |\n| `tasks update` | (none) | `--project`, `--area` (move) |\n| `tasks list` | `--project`, `--area` | (already correct) |\n| `projects create` | `--area` | (already correct) |\n\n## Precedence Rule\n\nWhen both `--project` and `--area` are specified:\n- Project takes precedence\n- Task goes into project\n- Task's area becomes project's area (inherited)\n- Print info message: \"Note: --project takes precedence, --area ignored\"\n\n## Sub-tasks\n\n- thingies-0wb: Replace --list with --project/--area in task creation\n- thingies-6zm: Add --project/--area to task update for moving\n- thingies-2hl: Support both names and UUIDs\n\n## Not Changing\n\n- `--heading` flag stays as-is (only makes sense with project)\n- URL scheme internals (`list` param) - just CLI surface changes\n- Filtering flags in `tasks list` - already correct","notes":"## Overview\n\nStandardize CLI flag naming for project and area references across all commands. Currently there's inconsistency between `--list` (ambiguous) and `--project`/`--area` (explicit).\n\n## Goals\n\n1. **Consistent terminology**: Use `--project` and `--area` everywhere, matching Things app UI\n2. **Clear semantics**: Users always know whether they're targeting a project or area\n3. **Flexible input**: Accept both names and UUIDs for convenience\n4. **Defined precedence**: When both specified, project wins (documented behavior)\n\n## Changes Required\n\n| Command | Current | Proposed |\n|---------|---------|----------|\n| `tasks create` | `--list` (ambiguous) | `--project`, `--area` |\n| `tasks update` | (none) | `--project`, `--area` (move) |\n| `tasks list` | `--project`, `--area` | (already correct) |\n| `projects create` | `--area` (name-only) | `--area` (name or UUID) |\n\n## Precedence Rule\n\nWhen both `--project` and `--area` are specified:\n- Project takes precedence\n- Task goes into project\n- Task's area becomes project's area (inherited)\n- Print info message: \"Note: --project takes precedence, --area ignored\"\n\n## Sub-tasks\n\n- thingies-0wb: Replace --list with --project/--area in task creation\n- thingies-6zm: Add --project/--area to task update for moving\n- thingies-2hl: Support both names and UUIDs\n\n## Breaking Change Strategy\n\nThe `--list` flag will be immediately removed (no deprecation period). This is a small CLI tool with few users, so clean break is preferred over compatibility shim.\n\n## Not Changing\n\n- `--heading` flag stays as-is (only makes sense with project)\n- URL scheme internals (`list` param) - just CLI surface changes\n- Filtering flags in `tasks list` - already correct\n\n## Related Work (out of scope for this epic)\n\n- Add `--add-tags` flag to tasks update (to match Python API)\n- Add `--append-notes`/`--prepend-notes` to tasks update\n- Update CLAUDE.md documentation after changes land","status":"open","priority":2,"issue_type":"epic","created_at":"2026-01-30T07:27:54.54008-05:00","updated_at":"2026-01-30T07:30:34.236899-05:00"}
{"id":"thingies-z7h","title":"Fix binary-packed date decoding in scanner.go","description":"## Problem\nThe `thingsDateToNullTime()` function in `internal/db/scanner.go` incorrectly treats `startDate` as an epoch-based timestamp. It's actually a binary-packed date.\n\n## Current (wrong) code\n```go\nconst thingsDateEpoch = 1636588800\nunixTime := int64(ts.Float64) + thingsDateEpoch\n```\n\n## Correct decoding\n`startDate` packs year/month/day into bits:\n- Bits 16-26: Year (11 bits)\n- Bits 12-15: Month (4 bits)\n- Bits 7-11: Day (5 bits)\n\n```go\ny_mask := 0x7FF0000  // bits 16-26\nm_mask := 0xF000     // bits 12-15\nd_mask := 0xF80      // bits 7-11\n\nyear := (startDate \u0026 y_mask) \u003e\u003e 16\nmonth := (startDate \u0026 m_mask) \u003e\u003e 12\nday := (startDate \u0026 d_mask) \u003e\u003e 7\n```\n\n## Verification\nTested with known values:\n- 132786048 → 2026-02-15 ✓\n- 132783616 → 2026-01-28 ✓\n- 132759680 → 2025-12-01 ✓\n\n## Files to change\n- `internal/db/scanner.go`: Replace `thingsDateToNullTime()` with binary decoding","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-01-27T19:40:59.400574-05:00","updated_at":"2026-01-30T07:21:30.096186-05:00","closed_at":"2026-01-30T07:21:30.096186-05:00","close_reason":"implemented"}
